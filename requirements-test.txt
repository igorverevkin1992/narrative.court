# Minimal dependency set to run the offline test suite (CI).
#
# The production requirements.txt pins forward-dated (June 2026) versions that
# will not resolve on a stock runner. The offline tests, however, only import
# this lean set -- the heavy SDKs (nicegui, openai, anthropic, google-genai,
# elevenlabs, httpx, soundfile) are lazy-imported inside functions and are never
# reached in offline mode, so they are not needed to run pytest.
pydantic>=2,<3
PyYAML>=6,<7
SQLAlchemy>=2,<3
tenacity>=8
lxml>=5
pytest>=8
pytest-asyncio>=0.23
